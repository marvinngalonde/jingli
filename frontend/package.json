{
  "name": "jingli-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "preview:web": "npm run preview --workspace=apps/web",
    "lint:web": "npm run lint --workspace=apps/web"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}